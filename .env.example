# Supabase Configuration
# ATENÇÃO: Em projetos Lovable, essas variáveis já estão configuradas automaticamente
# Este arquivo é apenas para referência sobre as variáveis de ambiente necessárias

# URL do projeto Supabase
VITE_SUPABASE_URL=your_supabase_project_url

# Chave pública/anon do Supabase (PODE ser exposta no frontend)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Chave de projeto (não secreta)
VITE_SUPABASE_PROJECT_ID=your_project_id

# IMPORTANTE:
# - A chave ANON é pública e pode ser exposta no frontend
# - A chave SERVICE_ROLE é secreta e só deve ser usada em Edge Functions
# - Em projetos Lovable, essas configurações já estão feitas automaticamente
# - Para deploy em outras plataformas, configure essas variáveis nos seus settings